# Edit these to point at YOUR MySQL. This file sits next to the jar and overrides
# the values bundled inside it. No need to touch the jar.
db.url=jdbc:mysql://localhost:3306/campuspay?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=Claret@07
db.driver=com.mysql.cj.jdbc.Driver
